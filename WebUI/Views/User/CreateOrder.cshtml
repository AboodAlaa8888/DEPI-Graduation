@model WebUI.ViewModels.OrderViewModel

@{
    ViewData["Title"] = "Create Order";
}

<div class="page-header mb-4">
    <h2 class="page-title">
        <i class="bi bi-plus-circle me-2"></i>Create New Order
    </h2>
    <p class="page-subtitle">Request nursing services</p>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card shadow-lg border-0">
            <div class="card-header">
                <h5 class="mb-0 fw-bold">
                    <i class="bi bi-clipboard-check me-2"></i>Order Information
                </h5>
            </div>
            <div class="card-body p-4">
                <form asp-controller="User" asp-action="CreateOrder" method="post">
                    <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>

                    <div class="row g-3">
                        <div class="col-12">
                            <label asp-for="Description" class="form-label fw-semibold">
                                <i class="bi bi-card-text me-1"></i>Description
                            </label>
                            <textarea asp-for="Description" class="form-control" rows="4" placeholder="Describe the nursing care needed"></textarea>
                            <span asp-validation-for="Description" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Gender" class="form-label fw-semibold">
                                <i class="bi bi-gender-ambiguous me-1"></i>Preferred Nurse Gender
                            </label>
                            <select asp-for="Gender" asp-items="ViewBag.Genders" class="form-select">
                                <option value="">-- Select Gender --</option>
                            </select>
                            <span asp-validation-for="Gender" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Duration" class="form-label fw-semibold">
                                <i class="bi bi-clock me-1"></i>Duration (Hours)
                            </label>
                            <input asp-for="Duration" class="form-control" type="number" step="0.1" placeholder="Enter duration in hours" />
                            <span asp-validation-for="Duration" class="text-danger small"></span>
                        </div>

                        <div class="col-12">
                            <label asp-for="Address" class="form-label fw-semibold">
                                <i class="bi bi-geo-alt me-1"></i>Service Address
                            </label>
                            <input asp-for="Address" class="form-control" placeholder="Enter the address where service is needed" />
                            <span asp-validation-for="Address" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="PatientAge" class="form-label fw-semibold">
                                <i class="bi bi-person me-1"></i>Patient Age
                            </label>
                            <input asp-for="PatientAge" class="form-control" type="number" step="0.1" placeholder="Enter patient age" />
                            <span asp-validation-for="PatientAge" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="OrderDate" class="form-label fw-semibold">
                                <i class="bi bi-calendar-event me-1"></i>Date & Time
                            </label>
                            <input asp-for="OrderDate" type="datetime-local" class="form-control" required />
                            <span asp-validation-for="OrderDate" class="text-danger small"></span>
                            <small class="text-muted">Select the preferred date and time for the service</small>
                        </div>
                    </div>

                    <input asp-for="NurseId" value="@ViewBag.NurseId" type="hidden" />
                    <input asp-for="PatientId" value="@ViewBag.PatientId" type="hidden" />

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        <a asp-controller="Admin" asp-action="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-x-circle me-2"></i>Cancel
                        </a>
                        <button type="submit" class="btn btn-success">
                            <i class="bi bi-check-circle me-2"></i>Create Order
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
